name: 4dhoi_solver
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pytorch=2.3.0
  - torchvision=0.18.0
  - pytorch-cuda=12.1
  - numpy=1.26.4
  - scipy=1.13.1
  - matplotlib>=3.9.0
  - seaborn=0.13.2
  - opencv=4.10.0
  - pillow=10.4.0
  - imageio=2.34.1
  - open3d=0.19.0
  - trimesh=4.6.4
  - pyyaml=6.0.2
  - omegaconf=2.3.0
  - tqdm=4.65.2
  - rich=13.7.1
  - ipython>=8.12.0
  - ipdb=0.13.13
  - cython=3.0.12
  - setuptools>=70.0
  - pytorch3d==0.7.6
  - pip
  - pip:
      - smplx==0.1.28
      - pyrender==0.1.45
      - gtsam==4.2
      - probreg==0.3.8
      - pykalman==0.10.2
      - opencv-contrib-python==4.11.0.86
      - pycocotools==2.0.8
      - plyfile==1.1.3
      - einops==0.8.1
      - addict==2.4.0
      - ConfigArgParse==1.7
      - terminaltables==3.1.10
      - six==1.17.0
      - tensorboardX==2.6.4
      - pytest==8.3.5
      - scikit-image==0.25.2
      # - git+git@github.com:zju3dv/GVHMR.git
      # Note: The following dependencies are REMOVED as they are not used:
      # - gvhmr (use local utility functions instead)
      # - gradio, fastapi, flask, dash (no web UI)
      # - jupyter, jupyterlab (no notebooks)
      # - oss2, gdown, opendatalab (no cloud storage)
      # - mmdet, mmcv, mmengine (multiperson not used)
      # - pytorch-lightning (no training)
      # - ultralytics, mediapipe (not used)
      # - mayavi, vtk (use open3d/pyrender instead)
      # - All web/audio/video processing tools not actually used

